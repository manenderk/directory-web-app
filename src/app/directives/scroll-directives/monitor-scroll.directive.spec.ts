import { MonitorScrollDirective } from './monitor-scroll.directive';

describe('MonitorScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new MonitorScrollDirective();
    expect(directive).toBeTruthy();
  });
});
