import { PreviewImagePipe } from './preview-image.pipe';

describe('PreviewImagePipe', () => {
  it('create an instance', () => {
    const pipe = new PreviewImagePipe();
    expect(pipe).toBeTruthy();
  });
});
